
version: '0.1.0'

targets:
  - id: catchall
    stalk:
      paths:
        - "**/*"
      base: master
    pounce:
      reviewers:
        - jerry-the-benevolent-dictator
      # when submitting a PR, jerry will not require another approver
      # but everyone else will require jerry's approval
      author_implicit_reviewer: true

