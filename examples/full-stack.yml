version: '0.1.0'

targets:
  - id: frontend
    stalk:
      paths:
        - "**/*.{js,ts}"
      base: master
    pounce:
      reviewers:
        - tom-smith
        - terry-burns
  - id: backend
    stalk:
      paths:
        - "api/**/*"
      base: master
    pounce:
      reviewers:
        - neil-xu
  - id: documentation
    stalk:
      paths:
        - "README.md"
        - "LICENSE"
      base: master
    pounce:
      reviewers: []

